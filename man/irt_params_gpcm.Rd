% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irt_params_helpers.R
\name{irt_params_gpcm}
\alias{irt_params_gpcm}
\title{Generate GPCM Item Parameters}
\usage{
irt_params_gpcm(
  n_items,
  n_categories,
  a_dist = "lnorm",
  a_mean = 0,
  a_sd = 0.25,
  b_dist = "normal",
  b_mean = 0,
  b_sd = 1,
  b_range = c(-2, 2),
  step_dispersion = 1,
  seed = NULL
)
}
\arguments{
\item{n_items}{Positive integer. Number of items.}

\item{n_categories}{Positive integer >= 2. Number of response categories
per item. Produces \code{n_categories - 1} step columns in \code{b}.}

\item{a_dist}{Character string for the discrimination distribution.
Currently only \code{"lnorm"} (log-normal) is supported. Default: \code{"lnorm"}.}

\item{a_mean}{Numeric. \code{meanlog} for the log-normal distribution.
Default: \code{0}.}

\item{a_sd}{Numeric. \code{sdlog} for the log-normal distribution.
Default: \code{0.25}.}

\item{b_dist}{Character string for the item-center distribution: either
\code{"normal"} (default) or \code{"even"}.}

\item{b_mean}{Numeric. Mean of item centers when \code{b_dist = "normal"}.
Default: \code{0}.}

\item{b_sd}{Numeric. SD of item centers when \code{b_dist = "normal"}.
Default: \code{1}.}

\item{b_range}{Length-2 numeric vector giving the minimum and maximum
item-center values. Only used when \code{b_dist = "even"}. Default: \code{c(-2, 2)}.}

\item{step_dispersion}{Non-negative numeric. SD of the within-item step
offsets drawn from \code{rnorm(0, step_dispersion)} and added to each item's
center. Default: \code{1.0}. \code{0} is allowed (all steps within an item equal
the item center — degenerate but useful for design exploration).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A named list with elements:
\describe{
\item{a}{Positive numeric vector of length \code{n_items}.}
\item{b}{Numeric matrix with \code{n_items} rows and
\code{n_categories - 1} columns. Steps are NOT sorted within row.}
}
}
\description{
Creates a list of discrimination (\code{a}) and step (\code{b}) parameters
suitable for passing to \code{\link[=irt_design]{irt_design()}} with \code{model = "GPCM"}.
}
\details{
The Generalized Partial Credit Model (Muraki, 1992) is partial-credit
family — like the Partial Credit Model, step parameters within each item
are NOT required to be ordered (the defining contrast with the Graded
Response Model). Unlike PCM, GPCM allows per-item discrimination: \code{a} is
a free positive vector rather than fixed at 1. See \code{\link[=irt_params_pcm]{irt_params_pcm()}}
for the Rasch-family alternative.
}
\examples{
# GPCM parameters: 15 items, 4 response categories
params <- irt_params_gpcm(n_items = 15, n_categories = 4, seed = 42)

# Tighter within-item step spread and a wider discrimination distribution
params <- irt_params_gpcm(
  n_items = 15, n_categories = 4,
  a_sd = 0.50, step_dispersion = 0.5, seed = 42
)

}
\seealso{
\code{\link[=irt_params_pcm]{irt_params_pcm()}} for the Rasch-family (a fixed at 1)
alternative, \code{\link[=irt_params_grm]{irt_params_grm()}} for the ordered-threshold polytomous
model, \code{\link[=irt_design]{irt_design()}} to use the generated parameters.
}
