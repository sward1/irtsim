% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irt_simulate.R
\name{irt_simulate}
\alias{irt_simulate}
\title{Run an IRT Monte Carlo Simulation}
\usage{
irt_simulate(
  study,
  iterations,
  seed,
  progress = TRUE,
  parallel = FALSE,
  se = TRUE,
  compute_theta = TRUE
)
}
\arguments{
\item{study}{An \link{irt_study} object specifying the design and study
conditions.}

\item{iterations}{Positive integer. Number of Monte Carlo replications.}

\item{seed}{Integer. Base random seed for reproducibility. Each iteration
uses \code{seed + iteration - 1}.}

\item{progress}{Logical. Print progress messages? Default \code{TRUE}.}

\item{parallel}{Logical. Run iterations in parallel using
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}? Default \code{FALSE}. Requires users to set
up a future plan (e.g., \code{future::plan(multisession)}) before calling.
See Details.}

\item{se}{Logical. Compute standard errors and confidence intervals for item
parameter estimates? Default \code{TRUE}. Set to \code{FALSE} for significant speed
improvement when only point estimates are needed (e.g., MSE, bias, RMSE
criteria). When \code{FALSE}, \code{se}/\code{ci_lower}/\code{ci_upper} columns in
\code{item_results} are \code{NA}.}

\item{compute_theta}{Logical. Compute EAP theta estimates and recovery metrics
(correlation, RMSE)? Default \code{TRUE}. Set to \code{FALSE} to skip the
\code{\link[mirt:fscores]{mirt::fscores()}} call when theta recovery is not needed. When \code{FALSE},
\code{theta_cor} and \code{theta_rmse} in \code{theta_results} are \code{NA} (but \code{converged}
is still tracked).}
}
\value{
An S3 object of class \code{irt_results} containing:
\describe{
\item{item_results}{Data frame with per-iteration item parameter
estimates (columns: iteration, sample_size, item, param, true_value,
estimate, se, ci_lower, ci_upper, converged).}
\item{theta_results}{Data frame with per-iteration theta recovery
summaries (columns: iteration, sample_size, theta_cor, theta_rmse,
converged).}
\item{study}{The original \link{irt_study} object.}
\item{iterations}{Number of replications run.}
\item{seed}{Base seed used.}
\item{elapsed}{Elapsed wall-clock time in seconds.}
\item{se}{Logical flag indicating whether SEs and CIs were computed.}
\item{compute_theta}{Logical flag indicating whether theta recovery
metrics were computed.}
}
}
\description{
Execute a Monte Carlo simulation study based on an \link{irt_study}
specification. For each iteration and sample size, data are generated,
missing values applied, the IRT model is fitted, and parameter estimates
are extracted and stored.
}
\details{
The returned \code{irt_results} object stores raw per-iteration estimates.
Use \code{\link[=summary.irt_results]{summary.irt_results()}} to compute performance criteria (bias, MSE,
RMSE, coverage, etc.) and \code{\link[=plot.irt_results]{plot.irt_results()}} to visualize results.
\subsection{Parallelization}{

When \code{parallel = TRUE}, the Monte Carlo loop over iterations is
parallelized via \code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}. Each parallel task
processes one iteration across all sample sizes sequentially.

\strong{Important:} This function does NOT configure a future plan. Users must
set their own plan before calling with \code{parallel = TRUE}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(future)
plan(multisession, workers = 4)  # or your preferred backend
results <- irt_simulate(study, iterations = 100, seed = 42, parallel = TRUE)
}\if{html}{\out{</div>}}

Without an explicit plan, future defaults to sequential execution
(no parallelism).
}

\subsection{Reproducibility contract}{

Reproducibility is guaranteed \strong{within a given dispatch mode}, not across
modes:
\itemize{
\item \strong{Serial mode} (\code{parallel = FALSE}) uses deterministic per-cell seeds
under the session's default RNG kind (Mersenne-Twister). Re-running with
the same base \code{seed} reproduces identical results bit-for-bit.
\item \strong{Parallel mode} (\code{parallel = TRUE}) delegates RNG management to
\code{future.apply::future_lapply(..., future.seed = TRUE)}, which assigns
each iteration a formally independent L'Ecuyer-CMRG substream. Re-running
with the same base \code{seed} reproduces identical results bit-for-bit across
parallel runs, including across different worker counts.
\item \strong{Across modes}, numerical results will differ because the two paths
use different RNG algorithms and different seeding strategies. Both are
statistically valid; the parallel path has the stronger formal guarantee
of independent substreams, which is the standard for Monte Carlo work.
}

Progress messages are suppressed in parallel mode (workers cannot stream to
stdout safely). Set \code{progress = FALSE} in serial mode to suppress messages
(they appear every 10\% of iterations).
}
}
\examples{
\donttest{
# Minimal example (iterations and sample sizes reduced for speed;
# use iterations >= 100 and 3+ sample sizes in practice)
design <- irt_design(
  model = "1PL", n_items = 5,
  item_params = list(b = seq(-2, 2, length.out = 5))
)
study <- irt_study(design, sample_sizes = c(200, 500))
results <- irt_simulate(study, iterations = 10, seed = 42)
summary(results)
plot(results)
}

}
\seealso{
\code{\link[=irt_study]{irt_study()}} for specifying study conditions,
\code{\link[=summary.irt_results]{summary.irt_results()}} and \code{\link[=plot.irt_results]{plot.irt_results()}} for analyzing output,
\code{\link[=irt_iterations]{irt_iterations()}} for determining the number of replications.
}
