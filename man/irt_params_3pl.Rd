% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irt_params_helpers.R
\name{irt_params_3pl}
\alias{irt_params_3pl}
\title{Generate 3PL Item Parameters}
\usage{
irt_params_3pl(
  n_items,
  a_dist = "lnorm",
  a_mean = 0,
  a_sd = 0.25,
  b_dist = "normal",
  b_mean = 0,
  b_sd = 1,
  b_range = c(-2, 2),
  c_shape1 = 5,
  c_shape2 = 17,
  seed = NULL
)
}
\arguments{
\item{n_items}{Positive integer. Number of items.}

\item{a_dist}{Character string for the discrimination distribution.
Currently only \code{"lnorm"} (log-normal) is supported. Default: \code{"lnorm"}.}

\item{a_mean}{Numeric. \code{meanlog} for the log-normal distribution.
Default: \code{0}.}

\item{a_sd}{Numeric. \code{sdlog} for the log-normal distribution.
Default: \code{0.25}.}

\item{b_dist}{Character string for the difficulty distribution. One of
\code{"normal"} or \code{"even"}. Default: \code{"normal"}.}

\item{b_mean}{Numeric. Mean of the normal distribution for \code{b}.
Only used when \code{b_dist = "normal"}. Default: \code{0}.}

\item{b_sd}{Numeric. SD of the normal distribution for \code{b}.
Only used when \code{b_dist = "normal"}. Default: \code{1}.}

\item{b_range}{Numeric vector of length 2. Range for evenly-spaced \code{b}
values. Only used when \code{b_dist = "even"}. Default: \code{c(-2, 2)}.}

\item{c_shape1}{Positive numeric. First shape parameter of the Beta
distribution used to generate \code{c}. Default: \code{5}.}

\item{c_shape2}{Positive numeric. Second shape parameter. Default: \code{17}.
The default \code{Beta(5, 17)} has \verb{E[c] ~= 0.227, SD ~= 0.087}, consistent
with typical four-option multiple-choice items.}

\item{seed}{Optional integer seed for reproducibility. If \code{NULL}
(default), the current RNG state is used.}
}
\value{
A named list with elements \code{a}, \code{b}, \code{c}, each a numeric vector
of length \code{n_items}.
}
\description{
Creates a list of discrimination (\code{a}), difficulty (\code{b}), and guessing
(\code{c}) parameters suitable for passing to \code{\link[=irt_design]{irt_design()}} with
\code{model = "3PL"}.
}
\examples{
# Default 3PL parameters for 30 items
params <- irt_params_3pl(n_items = 30, seed = 42)

# Custom guessing distribution (e.g., 5-option items, lower chance level)
params <- irt_params_3pl(
  n_items = 30, c_shape1 = 4, c_shape2 = 16, seed = 42
)

}
\seealso{
\code{\link[=irt_params_2pl]{irt_params_2pl()}}, \code{\link[=irt_params_grm]{irt_params_grm()}}, \code{\link[=irt_design]{irt_design()}}.
}
