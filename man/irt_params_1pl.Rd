% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irt_params_helpers.R
\name{irt_params_1pl}
\alias{irt_params_1pl}
\title{Generate 1PL Item Parameters}
\usage{
irt_params_1pl(
  n_items,
  b_dist = "normal",
  b_mean = 0,
  b_sd = 1,
  b_range = c(-2, 2),
  seed = NULL
)
}
\arguments{
\item{n_items}{Positive integer. Number of items.}

\item{b_dist}{Character string for the difficulty distribution. One of
\code{"normal"} or \code{"even"}. Default: \code{"normal"}.}

\item{b_mean}{Numeric. Mean of the normal distribution for \code{b}.
Only used when \code{b_dist = "normal"}. Default: \code{0}.}

\item{b_sd}{Numeric. SD of the normal distribution for \code{b}.
Only used when \code{b_dist = "normal"}. Default: \code{1}.}

\item{b_range}{Numeric vector of length 2. Range for evenly-spaced \code{b}
values. Only used when \code{b_dist = "even"}. Default: \code{c(-2, 2)}.}

\item{seed}{Optional integer seed for reproducibility. If \code{NULL} (default),
the current RNG state is used.}
}
\value{
A named list with a single element \code{b} (numeric vector of length
\code{n_items}). Note: no \code{a} is returned — 1PL fixes discrimination at 1
downstream rather than at generation time.
}
\description{
Creates a list of difficulty (\code{b}) parameters suitable for passing to
\code{\link[=irt_design]{irt_design()}} with \code{model = "1PL"}. The 1PL model is Rasch-family:
every item shares the same discrimination (fixed at \code{1}), so only \code{b}
is generated here — the \code{a = 1} contract is applied downstream in the
design's \code{validate_params} step.
}
\examples{
# Default 1PL parameters for 30 items
params <- irt_params_1pl(n_items = 30, seed = 42)

# Evenly-spaced difficulty across a wider range
params <- irt_params_1pl(n_items = 20, b_dist = "even", b_range = c(-3, 3))

}
\seealso{
\code{\link[=irt_params_2pl]{irt_params_2pl()}} for the free-discrimination binary alternative,
\code{\link[=irt_design]{irt_design()}} to use the generated parameters.
}
