% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irt_params_helpers.R
\name{irt_params_2pl}
\alias{irt_params_2pl}
\title{Generate 2PL Item Parameters}
\usage{
irt_params_2pl(
  n_items,
  a_dist = "lnorm",
  a_mean = 0,
  a_sd = 0.25,
  b_dist = "normal",
  b_mean = 0,
  b_sd = 1,
  b_range = c(-2, 2),
  seed = NULL
)
}
\arguments{
\item{n_items}{Positive integer. Number of items.}

\item{a_dist}{Character string for the discrimination distribution.
Currently only \code{"lnorm"} (log-normal) is supported. Default: \code{"lnorm"}.}

\item{a_mean}{Numeric. Mean of the log-normal distribution for \code{a}
(i.e., \code{meanlog}). Default: \code{0}.}

\item{a_sd}{Numeric. SD of the log-normal distribution for \code{a}
(i.e., \code{sdlog}). Default: \code{0.25}.}

\item{b_dist}{Character string for the difficulty distribution. One of
\code{"normal"} or \code{"even"}. Default: \code{"normal"}.}

\item{b_mean}{Numeric. Mean of the normal distribution for \code{b}.
Only used when \code{b_dist = "normal"}. Default: \code{0}.}

\item{b_sd}{Numeric. SD of the normal distribution for \code{b}.
Only used when \code{b_dist = "normal"}. Default: \code{1}.}

\item{b_range}{Numeric vector of length 2. Range for evenly-spaced \code{b}
values. Only used when \code{b_dist = "even"}. Default: \code{c(-2, 2)}.}

\item{seed}{Optional integer seed for reproducibility. If \code{NULL} (default),
the current RNG state is used.}
}
\value{
A named list with elements \code{a} (numeric vector) and \code{b} (numeric
vector), each of length \code{n_items}.
}
\description{
Creates a list of discrimination (\code{a}) and difficulty (\code{b}) parameters
suitable for passing to \code{\link[=irt_design]{irt_design()}}.
}
\examples{
# Default 2PL parameters for 30 items
params <- irt_params_2pl(n_items = 30, seed = 42)

# Evenly-spaced difficulty
params <- irt_params_2pl(n_items = 20, b_dist = "even", b_range = c(-3, 3))

}
\seealso{
\code{\link[=irt_params_grm]{irt_params_grm()}} for GRM parameters, \code{\link[=irt_design]{irt_design()}} to use the
generated parameters.
}
