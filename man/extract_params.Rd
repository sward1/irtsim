% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irt_simulate.R
\name{extract_params}
\alias{extract_params}
\title{Extract Item Parameter Estimates from a Fitted mirt Model (Internal)}
\usage{
extract_params(
  mod,
  design,
  estimation_model,
  iteration,
  sample_size,
  true_params,
  true_params_lookup,
  se = TRUE
)
}
\arguments{
\item{mod}{A fitted mirt object.}

\item{design}{An \code{irt_design} object (for true values and model type).}

\item{estimation_model}{Character string: "1PL", "2PL", or "GRM" (the
model that was fitted, which may differ from design$model).}

\item{iteration}{Integer iteration number.}

\item{sample_size}{Integer sample size.}

\item{true_params}{Data frame (used for schema).}

\item{true_params_lookup}{Named character vector mapping "\if{html}{\out{<item>}}_\if{html}{\out{<param>}}"
to true_value (for O(1) lookup instead of repeated vector scans).}

\item{se}{Logical. Extract standard errors and CIs? Default \code{TRUE}.}
}
\value{
Data frame with item_results columns.
}
\description{
Pulls point estimates, SEs, and CIs from a fitted mirt object and
returns them in long format matching the item_results schema.
}
\details{
Uses the list-based \code{coef(mod)} output (one matrix per item, keyed by
item name). Default \code{coef()} returns rows \verb{par, CI_2.5, CI_97.5};
SE is derived from CI width: \code{SE = (upper - lower) / (2 * z_0.975)}.
}
\keyword{internal}
