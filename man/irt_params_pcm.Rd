% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irt_params_helpers.R
\name{irt_params_pcm}
\alias{irt_params_pcm}
\title{Generate PCM Item Parameters}
\usage{
irt_params_pcm(
  n_items,
  n_categories,
  b_dist = "normal",
  b_mean = 0,
  b_sd = 1,
  b_range = c(-2, 2),
  step_dispersion = 1,
  seed = NULL
)
}
\arguments{
\item{n_items}{Positive integer. Number of items.}

\item{n_categories}{Positive integer >= 2. Number of response categories
per item. Produces \code{n_categories - 1} step columns in \code{b}.}

\item{b_dist}{Character string for the item-center distribution: either
\code{"normal"} (default) or \code{"even"}.}

\item{b_mean}{Numeric. Mean of item centers when \code{b_dist = "normal"}.
Default: \code{0}.}

\item{b_sd}{Numeric. SD of item centers when \code{b_dist = "normal"}.
Default: \code{1}.}

\item{b_range}{Length-2 numeric vector giving the minimum and maximum
item-center values. Only used when \code{b_dist = "even"}. Default: \code{c(-2, 2)}.}

\item{step_dispersion}{Non-negative numeric. SD of the within-item step
offsets drawn from \code{rnorm(0, step_dispersion)} and added to each item's
center. Default: \code{1.0}, consistent with mirt::simdata's polytomous
conventions and the PCM examples in Embretson & Reise (2000). \code{0} is
allowed (all steps within an item equal the item center — degenerate
but useful for design exploration).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A named list with elements:
\describe{
\item{a}{Numeric vector of length \code{n_items}, all 1 (Rasch family).}
\item{b}{Numeric matrix with \code{n_items} rows and
\code{n_categories - 1} columns. Steps are NOT sorted within row.}
}
}
\description{
Creates a list of discrimination (\code{a}, fixed at 1) and step (\code{b})
parameters suitable for passing to \code{\link[=irt_design]{irt_design()}} with \code{model = "PCM"}.
}
\details{
The Partial Credit Model (Masters, 1982) is a Rasch-family polytomous
model: every item shares the same discrimination (fixed at 1), and the
step parameters within each item are NOT required to be ordered. This
is the defining contrast with the Graded Response Model — see
\code{\link[=irt_params_grm]{irt_params_grm()}} for the ordered-threshold alternative.
}
\examples{
# PCM parameters: 15 items, 4 response categories
params <- irt_params_pcm(n_items = 15, n_categories = 4, seed = 42)

# Tighter within-item step spread (steps closer to the item center)
params <- irt_params_pcm(
  n_items = 15, n_categories = 4, step_dispersion = 0.5, seed = 42
)

}
\seealso{
\code{\link[=irt_params_grm]{irt_params_grm()}} for the ordered-threshold polytomous model,
\code{\link[=irt_design]{irt_design()}} to use the generated parameters.
}
